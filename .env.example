# Feature flags
ENABLE_NOTIFICATIONS=false

# Optional: Telegram bot credentials (used only if ENABLE_NOTIFICATIONS=true)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Optional: Email SMTP (used only if ENABLE_NOTIFICATIONS=true)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_TO=

